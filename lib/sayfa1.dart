import 'package:flutter/material.dart';

class SayfaBir extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('1. Sayfa', style: TextStyle(fontSize: 36.0, fontWeight: FontWeight.bold),),
    );
  }
}