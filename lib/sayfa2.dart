import 'package:flutter/material.dart';

class SayfaIki extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('2. Sayfa', style: TextStyle(fontSize: 36.0, fontWeight: FontWeight.bold),),
    );
  }
}