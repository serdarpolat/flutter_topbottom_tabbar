import 'package:flutter/material.dart';

class SayfaUc extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('3. Sayfa', style: TextStyle(fontSize: 36.0, fontWeight: FontWeight.bold),),
    );
  }
}